# 脚本任务管理系统 - 部署指南

## 📋 系统要求

- Python 3.7 - 3.11
- MySQL 8.0+
- Windows/Linux/MacOS

## 🚀 快速部署

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置数据库

编辑 `config.py`，修改数据库连接信息：

```python
SQLALCHEMY_DATABASE_URI = "mysql+pymysql://用户名:密码@localhost:3306/数据库名"
```

### 3. 初始化数据库

```bash
mysql -u root -p < init_db.sql
```

### 4. 启动应用

```bash
python run.py
```

访问：http://127.0.0.1:5000

默认账号：`admin / admin123`

## 📁 目录结构

```
SchedulerProject/
├── app/                    # 应用核心代码
│   ├── models/            # 数据模型
│   ├── routes/            # 路由控制器
│   ├── services/          # 业务逻辑
│   ├── templates/         # HTML模板
│   └── utils/             # 工具函数
├── static/                # 静态资源
│   ├── css/
│   └── js/
├── logs/                  # 日志目录
├── config.py              # 配置文件
├── run.py                 # 启动文件
├── requirements.txt       # 依赖列表
└── init_db.sql           # 数据库初始化脚本
```

## ⚙️ 配置说明

### 环境变量（可选）

```bash
# 数据库连接
export DATABASE_URL=mysql+pymysql://user:pass@localhost/scheduler_db

# Flask配置
export FLASK_ENV=production
export FLASK_APP=run.py
```

### 生产部署建议

1. 使用 Gunicorn 或 uWSGI
2. 配置 Nginx 反向代理
3. 启用 HTTPS
4. 定期备份数据库

## 📝 注意事项

1. 首次启动会自动创建管理员账号
2. 确保 logs 目录有写入权限
3. Python 路径建议使用绝对路径
4. 脚本文件需要有执行权限

## 🔧 常见问题

### 数据库连接失败
- 检查 MySQL 服务是否启动
- 验证数据库连接信息
- 确认数据库已创建

### 端口被占用
- 修改 config.py 中的端口配置
- 或关闭占用 5000 端口的程序

### 权限问题
- 确保应用有 logs 目录写入权限
- 脚本文件需要有执行权限


### 待办

   1. 允许配置任务之间的依赖关系：
    任务 B 在任务 A 成功后自动触发
    支持简单的 A → B → C 链式执行
    前端可视化展示依赖关系
    2. 📋 任务执行历史统计
    每个任务的执行次数、成功率、平均耗时
    按日/周/月维度的执行报告
    任务耗时分布图
   3. 🔄 失败重试机制
   任务失败后自动重试（可配置重试次数和间隔）
   支持指数退避策略（1s → 2s → 4s）
   在 ScriptTask 模型中增加 retry_count、max_retries、retry_delay 字段
   4. 🏷️ 任务分组/标签
   支持给任务打标签（如 爬虫、财务、数据同步）
   按标签筛选任务列表
   按标签批量启用/停用
   5. 失败短信发送提醒